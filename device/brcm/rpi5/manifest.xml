<?xml version="1.0" encoding="UTF-8"?>
<!-- Device HAL manifest for Raspberry Pi 5 - Android 16 (AIDL) -->
<!-- Minimal manifest - most HALs provide their own manifest fragments -->
<manifest version="2.0" type="device" target-level="202404">
    <!-- Audio (AIDL) -->
    <hal format="aidl">
        <name>android.hardware.audio.core</name>
        <version>2</version>
        <fqname>IModule/default</fqname>
    </hal>
    <hal format="aidl">
        <name>android.hardware.audio.effect</name>
        <version>2</version>
        <fqname>IFactory/default</fqname>
    </hal>

    <!-- Boot (AIDL) -->
    <hal format="aidl">
        <name>android.hardware.boot</name>
        <version>1</version>
        <fqname>IBootControl/default</fqname>
    </hal>

    <!-- Camera (AIDL) -->
    <hal format="aidl">
        <name>android.hardware.camera.provider</name>
        <version>2</version>
        <fqname>ICameraProvider/internal/0</fqname>
        <fqname>ICameraProvider/external/0</fqname>
    </hal>

    <!-- Composer/Graphics (AIDL) - allocator provided by service -->
    <hal format="aidl">
        <name>android.hardware.graphics.composer3</name>
        <version>3</version>
        <fqname>IComposer/default</fqname>
    </hal>

    <!-- Keymint (replaces Keymaster in Android 16) -->
    <hal format="aidl">
        <name>android.hardware.security.keymint</name>
        <version>3</version>
        <fqname>IKeyMintDevice/default</fqname>
    </hal>
    <hal format="aidl">
        <name>android.hardware.security.sharedsecret</name>
        <version>1</version>
        <fqname>ISharedSecret/default</fqname>
    </hal>
    <hal format="aidl">
        <name>android.hardware.security.secureclock</name>
        <version>1</version>
        <fqname>ISecureClock/default</fqname>
    </hal>

    <!-- Light (AIDL) -->
    <hal format="aidl">
        <name>android.hardware.light</name>
        <version>2</version>
        <fqname>ILights/default</fqname>
    </hal>

    <!-- Thermal (AIDL) -->
    <hal format="aidl">
        <name>android.hardware.thermal</name>
        <version>2</version>
        <fqname>IThermal/default</fqname>
    </hal>

    <!-- USB (AIDL) -->
    <hal format="aidl">
        <name>android.hardware.usb</name>
        <version>3</version>
        <fqname>IUsb/default</fqname>
    </hal>
    <hal format="aidl">
        <name>android.hardware.usb.gadget</name>
        <version>1</version>
        <fqname>IUsbGadget/default</fqname>
    </hal>

    <!-- Vibrator (AIDL - optional for Pi) -->
    <hal format="aidl">
        <name>android.hardware.vibrator</name>
        <version>2</version>
        <fqname>IVibrator/default</fqname>
    </hal>
</manifest>
