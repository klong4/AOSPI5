# Raspberry Pi 5 HAL Server SELinux Policy
# AIDL uses binder service_manager, not hwservice_manager

# Allow HAL domains to register and find AIDL services
# These are handled by hal_server_domain macros in individual HAL .te files
